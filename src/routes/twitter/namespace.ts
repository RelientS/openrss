import type { Namespace } from '../../types.js';

export const namespace: Namespace = {
  name: 'Twitter / X',
  url: 'https://x.com',
  description: 'Twitter (X) feeds via browser session reuse',
};
